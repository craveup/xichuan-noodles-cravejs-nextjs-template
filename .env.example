# Optional: explicitly override the Storefront API base URL.
# When omitted, the SDK derives the URL from the API key prefix (test → dev API, otherwise prod).
# NEXT_PUBLIC_API_URL=http://localhost:4000

NEXT_PUBLIC_CRAVEUP_API_KEY=dev-local-key

# Organization slug (merchant identifier)
NEXT_PUBLIC_ORG_SLUG=tomodachi-sushi

# Optional: Skip the organization landing page and route directly to a location slug
# Useful for single-location storefronts
# NEXT_PUBLIC_DEFAULT_LOCATION_ID=tomodachi-sushi-main-st

# Optional storefront site URL (used in links or admin UI)
NEXT_PUBLIC_STOREFRONT_URL=http://localhost:3000

# Google Maps (delivery address autocomplete / maps)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key
