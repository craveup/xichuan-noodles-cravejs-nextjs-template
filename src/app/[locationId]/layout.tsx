import React from "react";

const LocationLayoutPage = ({children}: {children: React.ReactNode}) => {
    return (
        <>
            {children}
        </>
    )
}

export default LocationLayoutPage;
